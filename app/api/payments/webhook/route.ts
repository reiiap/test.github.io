import { NextResponse } from "next/server";import { z } from "zod";import { prisma } from "@/lib/prisma";
const schema=z.object({provider:z.enum(['MIDTRANS','XENDIT','TRIPAY','DUITKU','MANUAL']),externalId:z.string(),status:z.enum(['PENDING','PAID','FAILED','EXPIRED','REFUNDED']),event:z.string().default('payment.updated')});
export async function POST(req:Request){const payload=await req.json().catch(()=>null);const parsed=schema.safeParse(payload);if(!parsed.success)return NextResponse.json({error:'Invalid webhook payload'},{status:400});const payment=await prisma.payment.update({where:{externalId:parsed.data.externalId},data:{status:parsed.data.status,paidAt:parsed.data.status==='PAID'?new Date():undefined,rawPayload:payload,transactionLogs:{create:{event:parsed.data.event,payload}}},include:{order:true}});if(parsed.data.status==='PAID')await prisma.order.update({where:{id:payment.orderId},data:{status:'PROCESSING'}});return NextResponse.json({ok:true})}
