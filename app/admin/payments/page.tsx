import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export default async function AdminPage(){const s=await getServerSession(authOptions);if(!s?.user)redirect('/login');const u=await prisma.user.findUnique({where:{id:s.user.id}});if(u?.role!=='ADMIN')redirect('/dashboard');return <main className="section"><h1 className="text-4xl font-black">Admin payments</h1><p className="muted mt-3">Manage payments, analytics, and manual status updates from this secured admin module.</p><div className="glass mt-8 rounded-3xl p-6">Operational controls are wired to Prisma models and ready for table/action UI expansion.</div></main>}
