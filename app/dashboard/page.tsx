import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import Link from "next/link";import { authOptions } from "@/lib/auth";
const cards=[['Orders','Track pending, processing, completed, and cancelled work.','/dashboard/orders'],['Payments','View payment history and download invoices.','/dashboard/payments'],['Tickets','Open support tickets and follow delivery notes.','/dashboard/tickets'],['Notifications','Read important project and payment updates.','/dashboard/notifications']];
export default async function Dashboard(){const session=await getServerSession(authOptions);if(!session?.user)redirect('/login');return <main className="section"><div className="glass rounded-[2rem] p-8"><p className="text-sky-300">Client command center</p><h1 className="mt-3 text-5xl font-black">Welcome, {session.user.name??'Developer'}.</h1><p className="muted mt-3">Manage purchases, tickets, invoices, and project delivery in one secure workspace.</p></div><div className="mt-6 grid gap-5 md:grid-cols-2">{cards.map(([k,v,h])=><Link href={h} key={k} className="glass rounded-3xl p-6 transition hover:-translate-y-1"><h2 className="text-xl font-bold">{k}</h2><p className="muted mt-2">{v}</p></Link>)}</div></main>}
