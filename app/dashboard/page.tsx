import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import Link from "next/link";import { authOptions } from "@/lib/auth";
const cards=[['Konverter','Mulai workflow Conversion Resource Pack Java Edition ke Bedrock Edition.','/converter'],['Riwayat Conversion','Pantau order yang pending, sedang diproses, selesai, atau gagal.','/dashboard/orders'],['Transaksi','Lihat riwayat pembayaran dan invoice untuk pembelian Coin.','/dashboard/payments'],['Bantuan','Buka tiket bantuan dan ikuti catatan dukungan.','/dashboard/tickets'],['Notifikasi','Baca update penting tentang Conversion dan transaksi.','/dashboard/notifications'],['Pengaturan Akun','Kelola profil dan informasi akun kamu.','/profile']];
export default async function Dashboard(){const session=await getServerSession(authOptions);if(!session?.user)redirect('/login');return <main className="section"><div className="glass rounded-[2rem] p-8"><p className="text-emerald-300">Dasbor ReiiKajurawa JvsB</p><h1 className="mt-3 text-5xl font-black">Selamat datang, {session.user.name??'Creator'}.</h1><p className="muted mt-3">Kelola Conversion, Saldo Coin, Transaksi, Riwayat, dan Bantuan dalam satu workspace aman.</p><div className="mt-5 inline-flex rounded-full border border-emerald-300/20 bg-emerald-300/10 px-4 py-2 text-sm text-emerald-100">Saldo Coin: —</div></div><div className="mt-6 grid gap-5 md:grid-cols-2">{cards.map(([k,v,h])=><Link href={h} key={k} className="glass rounded-3xl p-6 transition hover:-translate-y-1"><h2 className="text-xl font-bold">{k}</h2><p className="muted mt-2">{v}</p></Link>)}</div></main>}
