import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import Link from "next/link";import { authOptions } from "@/lib/auth";
export default async function Dashboard(){const session=await getServerSession(authOptions);if(!session?.user)redirect("/login");return <main className="section"><div className="glass rounded-[2rem] p-8"><p className="text-sky-300">Authenticated console</p><h1 className="mt-3 text-5xl font-black">Welcome, {session.user.name ?? "Developer"}.</h1><p className="muted mt-3">Your ReiiKajurawa Studio workspace is protected by persistent NextAuth sessions.</p></div><div className="mt-6 grid gap-5 md:grid-cols-3"><Card k="Provider" v={session.user.provider ?? "email"}/><Card k="Email" v={session.user.email ?? "Not connected"}/><Card k="Profile" v="Ready to edit"/></div><Link className="btn btn-primary mt-8" href="/profile">Edit profile</Link></main>}
function Card({k,v}:{k:string;v:string}){return <div className="glass rounded-3xl p-6"><p className="muted text-sm">{k}</p><p className="mt-2 text-xl font-bold">{v}</p></div>}
