import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export default async function Notifications(){const s=await getServerSession(authOptions);if(!s?.user)redirect('/login');const notes=await prisma.notification.findMany({where:{userId:s.user.id},orderBy:{createdAt:'desc'}});return <main className="section"><h1 className="text-4xl font-black">Notifications</h1><div className="mt-6 grid gap-4">{notes.map(n=><article className="glass rounded-3xl p-6" key={n.id}><p className="text-sky-300">{n.type}</p><h2 className="font-bold">{n.title}</h2><p className="muted">{n.message}</p></article>)}{notes.length===0&&<p className="muted">You're all caught up.</p>}</div></main>}
