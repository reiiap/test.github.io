import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export default async function Tickets(){const s=await getServerSession(authOptions);if(!s?.user)redirect('/login');const tickets=await prisma.ticket.findMany({where:{userId:s.user.id},include:{messages:true},orderBy:{updatedAt:'desc'}});return <main className="section"><h1 className="text-4xl font-black">Tickets</h1><div className="mt-6 grid gap-4">{tickets.map(t=><article className="glass rounded-3xl p-6" key={t.id}><h2 className="font-bold">{t.subject}</h2><p className="muted">{t.status} · {t.messages.length} messages</p></article>)}{tickets.length===0&&<p className="muted">No tickets yet.</p>}</div></main>}
