import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export default async function Orders(){const s=await getServerSession(authOptions);if(!s?.user)redirect('/login');const orders=await prisma.order.findMany({where:{userId:s.user.id},include:{invoice:true,payments:true},orderBy:{createdAt:'desc'}});return <main className="section"><h1 className="text-4xl font-black">Order history</h1><div className="mt-6 grid gap-4">{orders.map(o=><article className="glass rounded-3xl p-6" key={o.id}><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-bold">{o.title}</h2><p className="muted">{o.orderNumber}</p></div><span className="rounded-full bg-white/10 px-4 py-2 text-sm">{o.status}</span></div><p className="mt-4 text-sky-300">IDR {o.amount.toLocaleString('id-ID')}</p></article>)}{orders.length===0&&<p className="muted">No orders yet.</p>}</div></main>}
