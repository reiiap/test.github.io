"use client";import { useRouter } from "next/navigation";import { useState } from "react";import { toast } from "sonner";
export function ProfileForm({user}:{user:{name?:string|null;image?:string|null;email?:string|null}}){const[name,setName]=useState(user.name??"");const[image,setImage]=useState(user.image??"");const r=useRouter();async function save(e:React.FormEvent){e.preventDefault();const res=await fetch("/api/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,image})});const j=await res.json();if(res.ok){toast.success("Profile updated");r.refresh()}else{toast.error(j.error)}}return <form onSubmit={save} className="glass mt-8 max-w-xl rounded-3xl p-8 space-y-4"><label className="block text-sm font-bold">Name<input className="input mt-2" value={name} onChange={e=>setName(e.target.value)} required minLength={2}/></label><label className="block text-sm font-bold">Avatar URL<input className="input mt-2" value={image} onChange={e=>setImage(e.target.value)} placeholder="https://…"/></label><p className="muted text-sm">Signed in as {user.email??"phone-authenticated user"}</p><button className="btn btn-primary">Save profile</button></form>}
