"use client";
import { useState } from "react";import { toast } from "sonner";
export function ForgotPasswordForm(){const[email,setEmail]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const res=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const json=await res.json().catch(()=>({error:'Invalid server response'}));setLoading(false);res.ok?toast.success(json.message):toast.error(json.error)}return <div className="glass mx-auto max-w-md rounded-3xl p-8"><h1 className="text-3xl font-black">Reset password</h1><p className="muted mt-2">Submit your email. The API creates a secure reset token and is ready for email delivery integration.</p><form onSubmit={submit} className="mt-6 space-y-4"><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="Email" required/><button disabled={loading} className="btn btn-primary w-full">{loading?'Sending…':'Request reset'}</button></form></div>}
